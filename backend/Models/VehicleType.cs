namespace PROGI_Challenge.Api.Models
{
    // The numeric values are explicitly assigned(Common = 0, Luxury = 1) to ensure stable mapping for validations and fee rules.
    public enum VehicleType
    {
        Common = 0,
        Luxury = 1
    }
}
